# 文件空间后端配置示例
port: 8080         # 开发默认端口（前端 dev 的 /api/* 代理指向此处；0 = 随机端口）
name: ""           # 可选自定义节点名称

shared_folders:
  - path: "/home/ylxc/shared"
    name: "共享资料"

discovery:
  service_name: "_filespace._tcp"
  domain: "local."

monitor:
  interval_sec: 10
